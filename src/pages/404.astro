---
import Layout from '../layouts/Layout.astro'

const title = 'Page Not Found — MAD BUILDERS'
const description = '404 error - The requested page could not be found on MAD BUILDERS.'
---

<Layout title={title} description={description} noindex={true}>
	<div class="terminal">
		<div class="terminal-header">
			<div class="buttons">
				<span class="btn red"></span>
				<span class="btn yellow"></span>
				<span class="btn green"></span>
			</div>
			<div class="title">root@madbuilders:~$</div>
		</div>
		
		<div class="terminal-body">
			<div class="content">
				<div class="ascii-error">
					<pre>
██╗  ██╗ ██████╗ ██╗  ██╗
██║  ██║██╔═████╗██║  ██║
███████║██║██╔██║███████║
╚════██║████╔╝██║╚════██║
     ██║╚██████╔╝     ██║
     ╚═╝ ╚═════╝      ╚═╝
					</pre>
				</div>
				
				<div class="error-text">
					<div class="error-line">>>> ERROR: FILE NOT FOUND</div>
					<div class="error-line">>>> SYSTEM CANNOT LOCATE REQUESTED RESOURCE</div>
					<div class="error-line">>>> REDIRECTING TO MAINFRAME...</div>
				</div>
				
				<div class="actions">
					<a href="/" class="terminal-link">
						<span class="prompt">root@madbuilders:~$</span>
						<span class="command">cd /home</span>
					</a>
				</div>
			</div>
		</div>
	</div>
</Layout>

<style>
	body {
		background: #000;
		color: #00ff00;
		font-family: 'Courier New', 'Monaco', 'Menlo', monospace;
		margin: 0;
		padding: 0;
		overflow: hidden;
	}

	.terminal {
		height: calc(100vh - 20px);
		background: #000;
		border: 2px solid #00ff00;
		border-radius: 10px;
		margin: 10px;
		display: flex;
		flex-direction: column;
		box-shadow: 0 0 30px #00ff00;
	}

	.terminal-header {
		background: linear-gradient(90deg, #001100 0%, #002200 100%);
		height: 30px;
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 0 15px;
		border-bottom: 1px solid #00ff00;
		border-radius: 8px 8px 0 0;
	}

	.buttons {
		display: flex;
		gap: 8px;
	}

	.btn {
		width: 12px;
		height: 12px;
		border-radius: 50%;
		display: block;
	}

	.btn.red {
		background: #ff5f56;
	}

	.btn.yellow {
		background: #ffbd2e;
	}

	.btn.green {
		background: #27ca3f;
	}

	.title {
		color: #00ff00;
		font-size: 12px;
		font-weight: bold;
	}

	.terminal-body {
		flex: 1;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 40px 20px;
	}

	.content {
		text-align: center;
		max-width: 600px;
	}

	.ascii-error pre {
		color: #ff4444;
		font-size: 16px;
		line-height: 1.1;
		margin: 0 0 30px 0;
		text-shadow: 0 0 10px #ff4444;
	}

	.error-text {
		margin-bottom: 40px;
	}

	.error-line {
		color: #ff6666;
		font-size: 14px;
		margin: 8px 0;
		text-shadow: 0 0 5px #ff6666;
	}

	.actions {
		margin-top: 30px;
	}

	.terminal-link {
		display: inline-flex;
		align-items: center;
		gap: 10px;
		color: #00ff00;
		text-decoration: none;
		padding: 10px 20px;
		border: 1px solid #00ff00;
		border-radius: 5px;
		background: rgba(0, 255, 0, 0.1);
		transition: all 0.3s ease;
		font-family: inherit;
	}

	.terminal-link:hover {
		background: rgba(0, 255, 0, 0.2);
		box-shadow: 0 0 15px rgba(0, 255, 0, 0.3);
		transform: translateY(-2px);
	}

	.prompt {
		color: #00aa00;
		font-weight: bold;
	}

	.command {
		color: #00ff00;
		text-shadow: 0 0 5px #00ff00;
	}

	/* Mobile styles */
	@media (max-width: 768px) {
		.terminal {
			margin: 5px;
			height: calc(100vh - 10px);
		}

		.ascii-error pre {
			font-size: 12px;
		}

		.error-line {
			font-size: 12px;
		}

		.terminal-link {
			padding: 8px 16px;
			font-size: 14px;
		}
	}
</style>
